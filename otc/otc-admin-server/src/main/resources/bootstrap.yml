spring:
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled:  true
        serviceId:  otc-admin-server
    bus:
      enabled: true
      trace:
        enabled: true
  client:
    serviceUrl:
      defaultZone:  http://localhost:8761/eureka/
   